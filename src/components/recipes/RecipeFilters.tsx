import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function RecipeFilters() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Recipe Filters</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder recipe filters component.</p>
      </CardContent>
    </Card>
  );
}
