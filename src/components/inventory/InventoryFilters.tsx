import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function InventoryFilters() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Filters</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a placeholder inventory filters component.</p>
      </CardContent>
    </Card>
  );
}
